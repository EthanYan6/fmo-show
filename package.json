{
  "name": "fmo-show",
  "version": "1.0.0",
  "description": "FMO Show Application",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -f app.min.js style.min.css"
  },
  "devDependencies": {
    "uglify-js": "^3.17.4",
    "clean-css": "^5.3.2",
    "chokidar": "^3.5.3"
  }
}